version: '3.8'

services:
  llm_api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./data:/app/data
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G